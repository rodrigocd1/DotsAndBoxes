{
  "name": "@dots-and-boxes/game-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Lógica pura (sem UI) do jogo Dots and Boxes. 100% testável.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "prepreview": "npm run build",
    "postpreview": "git restore -- dist",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "start": "ts-node src/demo.ts",
    "apk": "npm run build && npx cap sync android && cd android && call .\\gradlew.bat assembleDebug && cd .. && node scripts/export-apk.mjs"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@tabler/icons": "^3.44.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.5",
    "flag-icons": "^7.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
